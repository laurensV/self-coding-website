nosana:
  description: Test Simple Secrets

global:
  image: ubuntu
  trigger:
    pull_request:
      branch:
        - main
    push: true
  secrets:
    - global.test  
  environment:
    TERM: xterm

jobs:
  - name: test-colors
    secrets:
      - SECRET_VALUE
    commands: 
      - echo $SECRET_VALUE
      - echo $test
